{
  "name": "@react-native-node-api-module-autolinking/monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "format": "bun run --filter '*' format",
    "lint": "bun run --filter '*' lint"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.19.0",
    "prettier": "2.8.8"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
